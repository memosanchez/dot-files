# macOS
.DS_Store

# Claude Code - ignore top-level .claude directory
.claude/settings.local.json

# Claude Code commands directory - don't track synced commands
claude/commands/.synced

# Homebrew
Brewfile.lock.json

# The synced global gitignore (git/.gitignore) hides CONTEXT.md everywhere;
# this repo tracks its own domain glossary deliberately.
!CONTEXT.md